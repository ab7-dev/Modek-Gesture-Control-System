# Modek-Gesture-Control-System
A real-time hand gesture system that detects finger flicks using MediaPipe and maps them to smart home commands (Light, Fan, AC, TV, Alarm). Uses adaptive filtering, FSM-based gesture detection, and WebSocket communication, with optional Arduino integration for hardware control.
